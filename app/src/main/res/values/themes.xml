<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BackGroundLocaton1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/dark_color2</item>
        <item name="iconTint">@color/dark_color2</item>
        <item name="android:textColor">@color/hintColor</item>
    </style>

    <style name="Theme.BackGroundLocaton1" parent="Base.Theme.BackGroundLocaton1" >
        <item name="colorPrimary">@color/dark_color2</item>
        <item name="iconTint">@color/dark_color2</item>
        <item name="android:textColor">@color/hintColor</item>
    </style>
</resources>